# Caller Studio

In-progress web app for organizing contra dances & programs.

## Structure

- **`web/`** — React frontend ([docs](web/README.md))
- **`supabase/`** — Database migrations and local Supabase config

## Quick Start

```bash
supabase start         # Start local Supabase
cd web && npm run dev  # Start frontend dev server
```
